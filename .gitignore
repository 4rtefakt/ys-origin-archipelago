# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# apworld build artifact (zip ys_origin/ to produce this)
*.apworld

# discovery output / RE scratch captures
flaglog*.csv
bless_*.csv
bless_*.txt

# overlay icons are extracted from the user's own game copy (game art) —
# generate locally with tools/extract_icons.py, don't commit
client/icons/

# native mod build output (CMake + fetched deps)
mod/build/

# OS / editor
Thumbs.db
.DS_Store

# local Archipelago install, RE scratch, etc. live OUTSIDE this repo
# (D:\Archipelago, D:\ghidra-work) — nothing to ignore here, noted for clarity

# room-mapping scene transition log (regenerate locally)
scenelog.jsonl

# third-party reference material (copyrighted) — local only, never published
fanmade_maps/
Ys-Origin-Guide.md
