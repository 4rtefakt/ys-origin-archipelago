# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# apworld build artifact (zip ys_origin/ to produce this)
*.apworld

# discovery output from tools/flaglog.py
flaglog*.csv

# overlay icons are extracted from the user's own game copy (game art) —
# generate locally with tools/extract_icons.py, don't commit
client/icons/

# OS / editor
Thumbs.db
.DS_Store

# local Archipelago install, RE scratch, etc. live OUTSIDE this repo
# (D:\Archipelago, D:\ghidra-work) — nothing to ignore here, noted for clarity
